spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres_catering_company:5433/mydatabase
    username: myuser
    password: secret
    client-order:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://postgres_catering_client:5434/myclientdatabase
      username: myclientuser
      password: secret
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: cateringClientService
server:
  port: 8080
